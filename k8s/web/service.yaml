apiVersion: v1
kind: Service
metadata:
  name: delerium-web
  namespace: delerium
  labels:
    app: delerium-web
spec:
  type: NodePort
  selector:
    app: delerium-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
