apiVersion: v1
kind: Service
metadata:
  name: delerium-web
  namespace: delerium
  labels:
    app: delerium-web
spec:
  type: ClusterIP
  selector:
    app: delerium-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
