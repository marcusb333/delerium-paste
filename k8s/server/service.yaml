apiVersion: v1
kind: Service
metadata:
  name: delerium-server
  namespace: delerium
  labels:
    app: delerium-server
spec:
  type: ClusterIP
  selector:
    app: delerium-server
  ports:
    - name: http
      port: 9090
      targetPort: 8080
      protocol: TCP
