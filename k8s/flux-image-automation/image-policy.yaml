apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: delerium-server
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: delerium-server
  policy:
    semver:
      range: ">=1.0.0"
