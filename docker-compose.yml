services:
  server:
    image: marcusb333/delerium-server:v1.2.0-alpha
    build:
      context: .
      dockerfile: ./server/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DELETION_TOKEN_PEPPER=${DELETION_TOKEN_PEPPER}
      - DATA_ENC_KEYRING_PATH=/data/keyring.json
      - DATA_ENC_ROTATION_DAYS=30
    volumes:
      - server-data:/data
    expose:
      - "8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 90s
    networks:
      - app-network

  web:
    image: marcusb333/delerium-web:v1.2.0-alpha
    build:
      context: .
      dockerfile: ./web/Dockerfile
    restart: unless-stopped
    depends_on:
      server:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ./client:/usr/share/nginx/html:ro
      - ./reverse-proxy/nginx-dev.conf:/etc/nginx/nginx.conf:ro
      - ./logs/nginx:/var/log/nginx
    networks:
      - app-network
    command: sh -c "rm -f /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"

volumes:
  server-data:
    driver: local

networks:
  app-network:
    driver: bridge
